# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# GSD planning and tooling runtime (local only, never committed)
.gsd/
.gsd-backups/
.gsd-id
.bg-shell/
.mcp.json
.claude/

# Editor
.vscode/.browse.c_cpp.db*
.DS_Store

# Python
__pycache__/
*.pyc
src.zip
EDGE-AUDIT.md
UPSTREAM-BUG-REPORT.md

# Local build/flash logs (generated by scripts/flash.sh and manual builds)
build_c6.log
flash_c6.log

# Local-only working notes (audits, reviews, road-test logs), not for remote
AUDIT-*.md
REVIEW-*.md
ROAD-TEST-*.md
Adam - *.md

# Local-only reverse-engineering notes, not for remote
HALDEX-KNOWLEDGE.md

# Local-only audit / review / road-test notes, not for remote
AUDIT-2026-07-13.md
REVIEW-2026-07-13.md
ROAD-TEST-BPK-CALIBRATION.md
Adam - 8-0-0-2 consolidated bug list.md

# Build / flash logs (local only)
build_c6.log
flash_c6.log

# Dropbox sync-ignore config (local only, never committed)
.dropboxignore

# Local UI dev helpers (not firmware): mock server, CDP render/verify scripts,
# and generated screenshots.
tools/
